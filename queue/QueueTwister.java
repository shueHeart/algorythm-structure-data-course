package queue;

public class QueueTwister {
	public static Queue twist (Queue queue, int N) {
		
		if (queue.size() == 0) return null;
		
		int realTwist = N % queue.size();
		
		
		
		Queue twistedQueue = new Queue<queue.>;
		
		
	}
}
